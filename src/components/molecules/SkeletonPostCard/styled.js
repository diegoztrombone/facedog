import styled from '@emotion/styled'
import { Box, Card } from '@mui/material'

export const S_Card = styled(Card)`
  width: 100%;
  overflow: visible;
`

export const S_CardMedia = styled(Box)`
  overflow: hidden;
`
